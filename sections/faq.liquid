<h1>{{ section.settings.headline }}</h1>

{% for block in section.blocks %}
  <h3>{{ block.settings.question }}</h3>
  <h4>{{ block.settings.answer }}</h4>
{% endfor %}

{% schema %}
  {
    "name": "faq",
    "tag": "section",
    "class": "faq",
    "presets": [
      {
        "name": "FAQ"
      }
    ],
    "settings": [
      {
        "type": "text",
        "id": "headline",
        "label": "Headline",
        "default": "this is the headline"
      }
    ],
    "blocks": [
      {
        "name": "question",
        "type": "question",
        "settings": [
          {
            "label": "question",
            "id": "question",
            "type": "text",
            "default": "marco?"
          },
          {
            "label": "answer",
            "id": "answer",
            "type": "text",
            "default": "polo"
          }
        ]
      }
    ]
  }
{% endschema %}